<resources xmlns:tools="http://schemas.android.com/tools">
  <!-- Compose provides in-app theming; keep the Android XML theme dependency-free. -->
  <style name="Theme.ListLens" parent="@android:style/Theme.DeviceDefault.NoActionBar">
    <item name="android:statusBarColor">@android:color/transparent</item>
  </style>
</resources>
